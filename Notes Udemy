1. Using JS we can enter the details in the DB. If those data is fectched on the target browser, the JS will get executed.
Ex . alert function to show some message
window location for redirecting to different page.

2. To avoid XXS attacks use strip tags before insert into DB.

3. value1,value2,value3) -- 

use of -- will comment the regular sql code. And our current injected code with the sent in as sql 
